# Utility Scripts

- `api/` — generates the optional private runtime key module from a local `.env` file.
- `contracts/` — stores compact structural contracts used to verify the React DOM.
- `github/` — validates, packages, and safely pushes the extension repository.
- `ollama/` — configures, starts, repairs, or removes the local Ollama helper.
