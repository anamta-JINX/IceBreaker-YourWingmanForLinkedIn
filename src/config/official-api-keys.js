// Safe public template. Copy this file to official-api-keys.js for a public/manual-key build.
// Never commit real provider keys.
export const OFFICIAL_API_KEYS = Object.freeze({
  groq: Object.freeze([]),
  openrouter: Object.freeze([])
});
